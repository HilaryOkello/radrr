{
  "name": "radrr-filecoin",
  "version": "1.0.0",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:testnet": "hardhat run deploy.ts --network filecoin_calibration"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0"
  }
}
